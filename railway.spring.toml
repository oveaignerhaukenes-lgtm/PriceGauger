[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python runtime_entrypoint.py spring_trade_engine/runtime/observer_runtime.py --interval-seconds 60 --window-minutes 120 --equilibrium-span 20"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
